import React from "react";
import ReactDom from "react-dom";
import App from "./App";

// const Greeting = () => {
//   let phrase = "hello";
//   return <h2 id="greeting">WE call just called to say {phrase}</h2>;
// };

ReactDom.render(<App />, document.getElementById("root"));
